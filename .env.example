# =============================================================================
# Apex AI Marketing - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the actual values:
#   cp .env.example .env
#   nano .env
#
# NEVER commit .env to version control!
# =============================================================================

# ── Database ─────────────────────────────────────────────────────────────
# These are used by docker-compose to construct the connection URLs.
# The DATABASE_URL and REDIS_URL are overridden in docker-compose.yml
# to use internal Docker networking, so these are mainly for reference
# or for running outside Docker.
DATABASE_URL=postgresql+asyncpg://apex:your-secure-password@db:5432/apex_ai_marketing
SYNC_DATABASE_URL=postgresql://apex:your-secure-password@db:5432/apex_ai_marketing
REDIS_URL=redis://redis:6379/0

# ── AI Engine (Anthropic Claude) ─────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-your-key-here
DEFAULT_MODEL=claude-sonnet-4-20250514
PREMIUM_MODEL=claude-opus-4-20250514

# ── Email (Resend) ───────────────────────────────────────────────────────
RESEND_API_KEY=re_your-key-here
FROM_EMAIL=hello@apexaimarketing.pro
FROM_NAME=Apex AI Marketing

# ── Authentication ───────────────────────────────────────────────────────
# Generate a secure secret: python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=generate-a-64-char-random-string-here
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440
ADMIN_USERNAME=artash
ADMIN_PASSWORD=change-this-secure-password

# ── Telegram Notifications ───────────────────────────────────────────────
# Bot: @LifeOsApex_bot
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id

# ── Website & Integrations ───────────────────────────────────────────────
SITE_URL=https://apexaimarketing.pro
CALENDLY_URL=https://calendly.com/artash-apex
WEB3FORMS_KEY=your-web3forms-key

# ── Brand Configuration ──────────────────────────────────────────────────
BRAND_NAME=Apex AI Marketing
BRAND_POSITIONING=AI Growth Infrastructure for predictable pipeline
DEFAULT_LANGUAGE=en
SUPPORTED_LANGUAGES=en,ru

# ── Market Configuration ─────────────────────────────────────────────────
PRIMARY_MARKET=dubai
TIMEZONE=Asia/Dubai
WORK_DAYS=sun,mon,tue,wed,thu

# ── File Storage ─────────────────────────────────────────────────────────
DELIVERABLES_DIR=deliverables
STATIC_DIR=static

# ── PostgreSQL (used by docker-compose for the db service) ───────────────
POSTGRES_USER=apex
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=apex_ai_marketing
